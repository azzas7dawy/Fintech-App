import 'package:flutter/widgets.dart';

class AppColors {
  AppColors._();

  // Primary Colors
  static const primary = Color(0xFF2B5BF5);
  static const primaryDark = Color(0xFF1D3A70);



  // Background Colors
  static const backgroundDark = Color(0xFF121212);
  static const backgroundLight = Color(0xFFF5F8FE);
  static const containerDark = Color(0xFF0D0D0D);

  // Surface / Card Colors
  static const surfaceDark = Color(0xff1E1F4B);

  // error color
  static const errorColor = Color(0xffF7931A);

  // Text Colors
  static const textPrimary = Color(0xff152C07); // كان greenText
  static const textSecondary = Color(0xff818898); // gray400
  static const textTertiary = Color(0xff666D80); // gray500
  static const textDisabled = Color(0xff5D5C5D); // gray

  // Border Colors / Dividers
  static const borderColor = Color(0xff272835); // gray700

  // Surface Variants
  static const surfaceVariant = Color(0xff1A1B25); // gray800

  // Accent / Warning / Utility
  static const accentOrange = Color(0xffF7931A); // lightOrange
  static const shadow = Color(0xFF0D0D12);

}
